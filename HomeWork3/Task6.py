# 6*.есть два целых неотрицательных числа A и В (ноль включительно)
# надо определить какое из них больше
# при решении нельзя пользоваться никакими операторами сравнения
# (ни IF, ни > < >= <=, вообще никакие сравнения)

import math
# Если в будет Rnage(min, max) то на выход будет True, если Range(max, min) То будет ноль или False. Добавил в выводе
# перевод в булиан для наглядности
first = 1
second = 2
total=0
for i in range(second,first):
    total+=i

print(bool(total))

